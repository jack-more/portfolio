An abstract flat vector logo mark built from exactly three identical quadrilateral cards stacked and offset, drawn as one dominant unified silhouette: each card is a wide rectangle with its top-right corner cut away at forty-five degrees like a filing tab notch, and each successive card sits shifted up and to the right by exactly one sixth of a card height so the stack forms a clean ascending staircase reading left to right, the consistent one-sixth offset being the distinctive element; the narrow uniform negative gaps between the three cards are left fully open so the ground shows through and the stack never reads as a solid mass; strictly geometric construction on a regular grid, intentional diagonal asymmetry along the rising axis, square corners except for the three cut notches; solid fills and clean paths only, no outlines of varying weight, no shadows, no dimensional depth, no gradients; deep near-black ink for the lowest card, muted gold for the middle card, and warm signal red for the topmost card so ascending the stack reads as ascending standing; centered and isolated on a plain background with generous even margin, internal detail limited to three shapes and two gaps for legibility at sixteen pixels; archival, ordered, confident register; no text.
